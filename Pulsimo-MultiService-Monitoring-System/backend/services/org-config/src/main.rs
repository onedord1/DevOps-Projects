// Org Config Service
// This service is currently integrated into the API Gateway
// for simplicity, but can be separated if needed.

fn main() {
    println!("Org Config Service - Currently integrated into API Gateway");
    println!("Run the API Gateway service instead");
}
